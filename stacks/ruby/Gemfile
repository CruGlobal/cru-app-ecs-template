source "https://rubygems.org"

gem "rack", "~> 3.1"
gem "puma", "~> 6.4"

# Want full Rails instead of this minimal Rack app? See the note at the top of
# config.ru — `rails new .` will scaffold it and replace these files.
